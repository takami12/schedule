<p id="notice"><%= notice %></p>
<h1>詳細画面</h1>
<table class="table table-hover">
  <div class="show">
    <tbody>         
      <%= form_with(model: @blog, local: true) do |form| %>
        <tr>
          <th>タイトル</th>
          <td><%= @blog.title %></td>
        </tr>
        <tr>
          <th>内容</th>
          <td><%= @blog.content %></td>
        </tr>
        <tr>
          <th>登録日時</th>
          <td><%= @blog.created_at %></td>
        </tr>
        <tr>
          <th>予定日</th>
          <td><%= @blog.start_time.strftime("%Y-%m-%d %H:%M:%S") %></td>
        </tr>
      <% end %>                       
    </tbody> 
  </div><!--/.show-->
</table>
<%= link_to "編集", edit_blog_path, class: 'btn btn-primary' %>    
<%= link_to '消去', blog_path(@blog.id), method: :delete, data: { confirm: '本当に?' } , class: 'btn btn-danger'%>
<%= link_to '戻る', blogs_path , class: 'btn btn-secondary'%>

<!-- 予約一覧画面として後で使う
<table class="table table-hover">
  <tr><th>Name</th><th>Age</th></tr>
  <tr><td>Yamada</td><td>16</td></tr>
  <tr><td>Suzuki</td><td>26</td></tr>
  <tr><td>Tanaka</td><td>36</td></tr>
-->